# 2026-09-11

## Website repo management (seimel_website) <!-- project: github.com/aseimel/website -->
- Remote `origin https://github.com/aseimel/website.git`. Live branch is **master** (not main); GitHub Pages source master `/` serves seimel.io. Do not create a `main` branch.
- Cleaned 2026-09-11: all 3 feature branches were already merged into origin/master via PRs #1-3; deleted local + remote feature branches. Local master fast-forwarded to live commit.
- gh CLI installed at `~/.local/bin/gh` (v2.100.0, user-local to respect Ansible-only host-change rule). Auth device-flow as aseimel (repo+workflow scopes). Gotcha: VM opens w3m on Enter and blocks; user must press `q`, enter the device code from their own machine at github.com/login/device.

## Party2D page revisions <!-- project: github.com/aseimel/website -->
- Added public abstract (708 parties, 65 countries, 1944-2025, econ + cultural scales, manifestos/media/expert surveys, uncertainty).
- Removed `Line styles: solid = economic...` note entirely per user.
- Release panel: static baked-in v0 download links only. **Never auto-fetch git.seimel.app on page load.** releases.js live-check removed entirely (script tag deleted) after user complaints.
- Button overflow fix: short asset labels (handles .csv.xz/.csv.gz/.zip/.tar.gz) + CSS white-space normal, overflow-wrap anywhere, min-width 0.
- Dark-mode chart fixes in app.js: legend color was missing on init (black on dark); dark-mode line recolor collapsed SPD red + CDU black to same off-white. Fixed with hue-preserving blend toward white + per-item legend colors + themed chip dots + dataset recolor on theme switch. Bumped query versions to bust cache (app.js v11, releases v5, css v6). Note: user still reported black legend after fix; likely cache/Pages delay, versions bumped again.

## git.seimel.app networking (durable infra fact)
- Split-horizon DNS: LAN resolves `192.168.168.240`, public resolves `91.99.50.99`. Any page-load fetch to the hostname triggers the iOS local-network permission prompt. Standing rule: page must contact nothing but same-origin seimel.io on load; homelab contact only on explicit user click (downloads/repo links, which stay on git.seimel.app only).
- Gitea API CORS already allows https://seimel.io; failure cause was LAN-IP fetch, not CORS.

## Sources table (standing user preferences)
- User demands real numbers, no placeholders: got angry at "part of CHES family*" and "see note*". Never use placeholder cells.
- CHES harmonized counts from party2d `data/processed/expert.csv` (project CHES, split by country/year): Europe 319 (1999-2024), Canada 5 (2023), Latin America 53 (2020), Israel 13 (2021-2022). Sums to paper's 390 exactly. Raw input filenames confirm years (trend 1999-2019 + 2024 final; CHES_CA2023; ches_la_2020; CHES_ISRAEL 2021_2022).
- Label is **V-Party**, never V-Dem (user emphatic). Correct link: `https://www.v-dem.net/data/v-party-dataset/` (www.v-party.org is dead, verified 200 on the v-dem.net URL).
- Dropped the CHES footnote per user request; keep general coverage note only.
